# Use the flag "-i" before each of your interfaces, like so:
#  INTERFACES="-ieth1 -iwlan0"
INTERFACES="-iwifi_ext"

# Use the flag "-D" before each driver, like so:
#  DRIVERS="-Dwext"
DRIVERS="-Dnl80211"

# Other arguments
#   -s   Use syslog logging backend
OTHER_ARGS="-s"
